#!/usr/bin/env bash

password="$1"

if [ -z "$password" ]; then
    echo "Usage: $0 <password>"
    exit 1
fi

node_modules/.bin/pm2 start src/bin/server.ts -- "$password"

node_modules/.bin/pm2 logs --raw
