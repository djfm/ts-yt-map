#!/usr/bin/env bash

password=$1

if [ -z "$password" ]; then
    echo "Usage: $0 <password>"
    exit 1
fi

docker compose build server
docker compose run --service-ports -e NODE_ENV=production-docker --rm server bash docker-server "$password"
