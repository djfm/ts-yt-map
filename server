#!/usr/bin/env bash

password=$1

if [ -z "$password" ]; then
    echo "Usage: $0 <password>"
    exit 1
fi

docker compose up server --build -d
docker compose run -e NODE_ENV=production-docker -e LOG=show --rm docker-server postgres-server bash docker-server "$password"
